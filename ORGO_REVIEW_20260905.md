# ORGO 안정화·Telegram 편집 검토 — 2026-09-05

실제로 재현되는 결함을 우선 수정했다. 프런트·API·Telegram을 병렬 검토한 뒤 통합 회귀를 수행했다. 새 플랫폼 계정 연결은 사용자의 요청에 따라 나중에 진행한다.

## 사이트 검토 결과

| 범위 | 확인 및 수정 |
|---|---|
| 홈·리포트·흐름맵·버블맵·트리맵 | 날짜/실시간 자료 요청 검토. 4개 화면에서 이전 날짜 응답이 새 날짜 화면을 덮는 문제 수정. 빈 스냅샷에서 이전 차트가 남지 않도록 처리 |
| 종목 상세·기사·검색·캘린더 | 당일 보강·근거·자동완성·월 이동 검토. 기존 이유 검증·동시성 회귀 확인 |
| 로그인·관리자 | 외부 이동으로 해석되는 역슬래시와 헤더 제어문자 차단. 비정상 세션/JSON/토큰 입력 처리 |
| 관리자 수정 전파 | GitHub HTTP 204 성공 인식. 영문 포함 6자리 종목을 저장·삭제·이력 반영까지 지원. 환경변수 공백 정규화 일치 |
| 관심종목 동기화 | KV 명령 실패를 저장 성공으로 표시하지 않음. 비정상 별점 입력 처리 |
| 시장 시세 API | 수집 실패나 날짜/장 상태 누락을 정상 빈 데이터로 캐시하지 않고 502/no-store 반환 |
| 발행실 | 디자인 유지. 모든 주제·5채널·다운로드·편집·모바일 가로 넘침 재검증 |

전체 사이트가 무결함이라는 의미는 아니다. 실제 사용자 계정의 로그인/관심종목/관리자 데이터 쓰기는 수행하지 않았다. 인증 정책·저장키·개인 데이터 저장 구조·사이트 집계 기준은 유지했다.

## Telegram 편집 구성

| 슬롯 | 내용 | 이미지 |
|---|---|---|
| 장전 | 해외 지수 조회 + 최근 확정 국내 데이터 + 개장 후 확인점 | 텍스트 |
| 09:30 주도주 | 거래대금×상승률 상위 주도주와 확인된 설명 | 주도주 카드 |
| 10:00 테마 | 주도 테마와 확산 확인점, 통계/지도 모집단 구분 | 버블맵 + 트리맵 |
| 점심·경보 | 지수/종목 분포, 기준 도달과 실제 발동 보도를 구분 | 텍스트 |
| 마감 | 확정 수집 종목 변화 + 사이트 캘린더 대장 | 대장 카드 + 버블맵 |
| 저녁 | 비교일 대비 신규/재등장 종목과 확인된 보도 | 텍스트 |
| 주간 | 최근 7일 집계와 이번 주 캘린더 기록 구분 | 상위 3개 섹터/테마 + 일별 대장 |
| 월간 | 최근 31일 집계와 해당 월 캘린더 구분 | 상위 3개 섹터/테마 + 캘린더 |

마감·저녁에는 날짜가 일치하는 `is_final:true` ORGO 스냅샷이 필요하다. 장전 국내 복기는 최근 확정본이 없으면 생략한다. 대장은 더 넓은 모집단을 사용하는 기존 사이트 캘린더 결과를 재사용하며, 별도 +15% 수집 통계와 섞어 재계산하지 않는다. 지도는 전체 수집 종목, 통계는 +15% 기준임을 표시한다.

발송 일정·중복 방지 기록·시장 감지 조건은 유지했다. 정기 7개 슬롯과 시장 감시의 AI 한 줄 호출을 제거하고, 워크플로에 불필요한 Anthropic 키 전달도 제거했다. 긴 이름 때문에 주간 카드 오른쪽 열이 잘리는 현상은 실제 렌더로 확인해 열 너비와 이름/수치 배치를 수정했다. 테스트 게시물은 운영 채널에 보내지 않았다.

## 직접 게시와 비용

Telegram은 기존 연결과 자동 일정을 사용한다. 발행실 원고/이미지도 자동 생성된다. Instagram·Threads는 계정·게시 권한 연결 후 자동 게시 경로를 사용할 수 있다. Kakao·Toss는 대상과 실제 사용 가능한 발행 경로 확인이 필요하다. 계정 연결은 사용자가 나중에 진행하기로 했다.

발행실/Telegram 기본 운영에는 유료 LLM 호출이 필요하지 않다. 사이트 종목별 AI 이유 정제는 별도 기능이며 현재 Anthropic 연결과 기사 기반 대체 경로를 유지했다. 기존 운영 로그에서 잔액 부족을 확인했지만 현재 결제 화면 잔액을 조회한 것은 아니다. 근거 없는 이유를 만들어 채우지 않는다.

OpenAI API로 이유 정제를 대체할 수 있지만 API 키·별도 사용료와 결과 비교 검증이 필요하다. ChatGPT 구독 로그인으로 Codex를 쓰는 방식과 서버에서 API 키로 호출하는 방식은 과금 경로가 다르다. ChatGPT 구독이 일반 OpenAI API 사용료를 자동으로 포함하지 않는다. 기본 운영은 추가 충전 없이 유지하고, AI 이유 정제가 필요할 때 별도로 비교하면 된다.

공식 근거: [Codex 인증·API 키 과금](https://learn.chatgpt.com/docs/auth), [OpenAI API 모델](https://developers.openai.com/api/docs/models).

## 검증

- Python: 이유 품질, 이력 빌드, API 안정성, track, 관리자 수정 테스트.
- Node: 마케팅/Telegram 발송, 날짜 응답 역전, 시장 경보 문구·중복 방지, API 동시성, 홈, 관리자 escaping.
- Playwright: 발행실 주제·5채널·다운로드·편집·모바일 검증. Telegram 실제 이미지와 잘림 검사.
- 정기 Telegram 7개 슬롯 dry-run. 실제 발송·유료 API 호출 없이 확인.
- 새 재현 테스트를 회귀 워크플로에 포함. API 테스트는 외부 연결을 차단하고 영문 종목 수정/복원은 임시 디렉터리에서 검증.

날짜 경쟁조건 테스트는 변경 전 실제 함수에서 실패하고 수정 후 통과했다. 운영 배포와 원격 실행 결과는 아래에 기록한다.
